# Animation Plans

Generated by the `improve-animations` skill. Audit at commit `2880b1d2` (line refs are against the current working tree, which has uncommitted changes). Lens: **restrained / professional**.

| # | Title | Severity | Status |
|---|-------|----------|--------|
| 001 | [Reduced-motion coverage](001-reduced-motion-coverage.md) | MEDIUM | TODO |
| 002 | [Consolidate base.css keyframe easings onto tokens](002-basecss-motion-tokens.md) | LOW | TODO |
| 003 | [Easing token + budget durations for overlay primitives](003-overlay-motion-cohesion.md) | MEDIUM | TODO |
| 004 | [Chat panel slides in from its trigger (first `motion` use)](004-chat-panel-slide-in.md) | MEDIUM | TODO |

## Recommended execution order

1. **001** (reduced-motion) — pure a11y addition; no default-user behavior change; ship first.
2. **002** (base.css token consolidation) — pure refactor; non-overlapping with 001 (001 appends a media query, 002 edits keyframe lines), so they can land together.
3. **003** (overlay cohesion) — one global CSS rule + ~10 duration edits across the overlay primitives; independent of 001/002.
4. **004** (chat panel) — largest change; first use of `motion`. Do last so the cheap, safe wins ship first.

## Dependencies

- **004** depends on `motion` (installed in Phase 0) and on `ResizableHandle` gaining `onResizeStart`/`onResizeEnd` (included in the plan).
- **003** depends on the `--ease-out` token (added in Phase 0).
- **001 / 002 / 003** are mutually independent (different files, or non-overlapping sections of `base.css`).

## Out of scope (kept in the audit, not planned this round)

Corrective: command-palette animation removal (HIGH-by-rule, subtle felt impact), accordion height-keyframe interruptibility, progress-bar `scaleX`, `scale(0)` in `completion-badge`, button press scale, sheet/sidebar easing tokens, rainbow-shimmer cohesion, typing-dot stagger, ungated `hover:animate-spin`, nav-menu 350ms. Additive (missed opportunities): content-body swap fade (MO2), outline-sidebar reveal (MO3), folder chevron rotate (MO4), tab-content swap fade (MO5). Revisit after this batch lands and is reviewed.

Each plan is self-contained — execute with any agent, then gate the resulting diff with `review-animations` (Block / Approve).
